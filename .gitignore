# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/

# OS / editors
.DS_Store
.idea/
.vscode/

# 조사 산출물 (사용자 프로젝트 폴더에 생성되는 것 — 리포에 커밋 금지)
*.jsonl
!tests/fixtures/*.jsonl
details/
output/
survey-*/
screening.jsonl
sole-profile.md
*-workspace/

# API 키·비밀 (보조금24 등 — 어떤 형태로든 커밋 금지)
api_key
*.key
.env
.env.*
secrets/

# 로컬 도구
.playwright-mcp/
scratch/

# 로컬 전용 (히스토리에서 제거됨)
docs/
