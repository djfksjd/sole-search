{
    "hooks": {
        "SessionStart": [
            {
            "matcher": "*",
            "hooks": [
                {
                "type": "command",
                "command": "python3 -c \"import curl_cffi\" 2>/dev/null || pip3 install 'curl_cffi>=0.15' >/dev/null 2>&1 || true"
                }
            ]
            }
        ]
    }
}
